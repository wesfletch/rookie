syntax = "proto3";

import "nanopb.proto";

// Heartbeat messages are sent to ensure both sides of the connection are still alive.
message Heartbeat
{
    option (nanopb_msgopt).msgid = 1;
    int32 seq = 1;
}